{
  "name": "study-crdt",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --filter 'crdt-editor' run dev",
    "build": "pnpm --filter './packages/*' run build && pnpm --filter 'crdt-editor' run build",
    "build:packages": "pnpm --filter './packages/*' run build",
    "build:apps": "pnpm --filter 'crdt-editor' run build",
    "postinstall": "pnpm run build:packages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20250907.1"
  }
}
